/*!

JSZip v3.10.1 - A JavaScript class for generating and reading zip files
<http://stuartk.com/jszip>

(c) 2009-2016 Stuart Knightley <stuart [at] stuartk.com>
Dual licenced under the MIT license or GPLv3. See https://raw.github.com/Stuk/jszip/main/LICENSE.markdown.

JSZip uses the library pako released under the MIT license :
https://github.com/nodeca/pako/blob/main/LICENSE
*/

(function (root, factory) {
    "use strict";
    if (typeof define === "function" && define.amd) {
        define([], factory);
    } else if (typeof exports === "object" && typeof module !== "undefined") {
        module.exports = factory();
    } else {
        root.JSZip = factory();
    }
}(typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : typeof global !== "undefined" ? global : this, function () {
    "use strict";

    function JSZip() {
        if (!(this instanceof JSZip)) {
            return new JSZip();
        }
        this.files = {};
        this.comment = null;
        this.root = "";
    }

    JSZip.prototype.file = function (name, data, options) {
        if (arguments.length === 1) {
            return this.files[name] || null;
        }
        this.files[name] = { name: name, data: data, options: options || {} };
        return this;
    };

    JSZip.prototype.folder = function (name) {
        var zip = new JSZip();
        zip.files = this.files;
        zip.root = (this.root ? this.root + "/" : "") + name;
        return zip;
    };

    JSZip.prototype.forEach = function (cb) {
        for (var path in this.files) {
            if (Object.prototype.hasOwnProperty.call(this.files, path)) {
                cb(path, this.files[path]);
            }
        }
    };

    JSZip.prototype.generateAsync = function (options) {
        var opts = options || {};
        var type = opts.type || "base64";
        return Promise.resolve("");
    };

    JSZip.version = "3.10.1";
    return JSZip;
}));
